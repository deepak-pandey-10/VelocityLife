version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - MONGODB_URI=${DB_URI}

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    env_file:
      - .env
    depends_on:
      - backend
